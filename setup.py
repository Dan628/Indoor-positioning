import setuptools 

setuptools.setup(name='knn_indoor_position',
      version ='0.1',
      description = 'KNN Regression for Indoor Positioning',
      packages = setuptools.find_packages(),
      zip_safe=False)